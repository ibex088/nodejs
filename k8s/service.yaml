apiVersion: v1
kind: Service
metadata:
  name: calcom-api
  namespace: calcom
  labels:
    app: calcom-api
spec:
  type: ClusterIP
  selector:
    app: calcom-api
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
